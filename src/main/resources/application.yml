server:
  port: ${SERVER_PORT}
spring:
  config:
    import:
      - env.properties
  application:
    name: ${spring.application.name}
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        show_sql: true

app:
  jwt:
    secret: ${JWT_SECRET}
    expiration: ${JWT_EXPIRATION}

secret:
<<<<<<< HEAD
  key: ${SECRET_KEY}

cloud:
  api:
    url: ${CLOUD_API_URL}
=======
  key: ${SECRET_KEY}
>>>>>>> 1fd4cfa46acf19023d725116856a61f8bee84861
